import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DesigndesobrancelhasPage } from './designdesobrancelhas.page';

describe('DesigndesobrancelhasPage', () => {
  let component: DesigndesobrancelhasPage;
  let fixture: ComponentFixture<DesigndesobrancelhasPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DesigndesobrancelhasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
