import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DrenagemlinfaticaPage } from './drenagemlinfatica.page';

describe('DrenagemlinfaticaPage', () => {
  let component: DrenagemlinfaticaPage;
  let fixture: ComponentFixture<DrenagemlinfaticaPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DrenagemlinfaticaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
